function addition(x, y) {
    return (x + y);
}
let add = addition(3, 4);
console.log(add);

function subtraction(x, y) {
    return (x - y);
}
let sub = subtraction(3, 4);
console.log(sub);

function generation(x, y) {
    return (x * y);
}
let gen = generation(3, 4);
console.log(gen);

function division(x, y) {
    return (x / y);
}
let div = division(3, 4);
console.log(div);

